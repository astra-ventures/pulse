#!/usr/bin/env bash
# Pulse CLI — autonomous cognition engine
# Portable: uses python3 from PATH by default.
set -euo pipefail
PYTHON_BIN="${PULSE_PYTHON:-python3}"
exec "$PYTHON_BIN" -m pulse.src.cli "$@"
