# OpenClaw Webhook Token
# Get this from: cat ~/.openclaw/config.yaml | grep webhookToken
OPENCLAW_HOOKS_TOKEN=your-webhook-token-here

# Optional: Override webhook URL (default: http://host.docker.internal:8080/hooks/agent)
# OPENCLAW_WEBHOOK_URL=http://your-openclaw-host:8080/hooks/agent
